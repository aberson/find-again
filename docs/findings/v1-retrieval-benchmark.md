# find-again v1 retrieval benchmark

_Generated by `scripts/benchmark.py` (`uv run python scripts/benchmark.py`). Regenerated on every run; results are reported honestly -- a missed target is recorded as a miss._

- Generated: 2026-07-29T03:20:39Z
- Overall verdict: **FAIL**
- Target root (read-only): `C:\Users\abero\dev`
- Sample roots: `docs`, `.claude/rules`, `.claude/task-state`
- Exclusions: built-in secret globs + operator `**/*.env`, `**/secrets*`; `max_file_kb = 512`; git-ignored files skipped.
- Index: **1528 documents** from **1293 files**, built in **6.76s** (one-time; queries run against the built index).

## Secret-scanner fix (fa6)

This step tuned the generic **high-entropy fallback** in `config.py` so it no longer false-positives on the long path / kebab / snake identifier strings that fill dev-memory docs. A run is flagged only when it has a genuinely-random body (>= 32 non-separator chars, mixed lower/upper/digit) above a raised entropy floor (>= 4.6 bits/char), is NOT separator-dominated (kebab / snake / dotted identifiers skip), and does NOT read as a nested path. **Security note (fa6 follow-up):** an interim `/`/`\` path-skip was REMOVED -- it opened a real false-negative, letting a standard-base64 secret (whose alphabet includes `/`) slip into the index un-scanned, which is worse than the path-drop it prevented (a secret indexed > a doc dropped). Slashes are now scored like any other char, and `/` `\` stay inside the token class so a slash-bearing secret is measured as ONE high-entropy run rather than split into evading fragments. Shallow readable paths fall under the entropy floor; DEEPLY-nested mixed-case paths that edge over it (`.../CurrentVersion/Uninstall/...`, timestamped `.judge-motion` dirs) are caught by a **nested-path guard** -- >= 3 `/`/`\`-delimited segments each holding a 4-letter lowercase word, which a random base64 blob does not have (measured false-negative rate on 500k random base64 secrets: ~0.03-0.07%, 0% for base64url). Every SPECIFIC secret pattern (connection-string credential, vendor API token, assigned-credential, PEM header, AWS key, JWT) is UNCHANGED, and the safety invariants below still hold. **Effect:** `docs/lessons-learned.md` and the other files previously dropped by the `high-entropy-token` heuristic are now indexed -- the earlier `secret-scan/secret-content` over-exclusion is gone. Q1's remaining top-5 miss is now a ranking/corpus-composition item (a large canonical doc outranked by focused derivative docs under BM25), the same class as Q5 -- deferred to v1.1, NOT a secret-scanner gap.

## Methodology

The harness constructs a `find_again.config.Config` pointing `root` at the real dev workspace and indexes the sample roots into a **disposable in-memory / temp database** -- it reads the workspace but writes nothing into it (never `<dev-root>/.find-again`). It then runs each pre-registered query via `find_again.search.search(..., limit=5)` and records whether the expected source appeared in the top five and the wall-clock time of the `search()` call (time-to-recovery; the index is pre-built, as it is in real incremental use). Exclusion and refresh are exercised on throwaway fixture trees so the real workspace is never mutated. Re-run with `uv run python scripts/benchmark.py` (override the root via `FIND_AGAIN_BENCH_ROOT`).

## Pre-registered targets

| # | Query | Expected source | Recovered (top-5)? | Rank | Time |
|---|---|---|---|---|---|
| 1 | never dump secret file contents | `docs/lessons-learned.md` | **NO** | - | 2.52 ms |
| 2 | 83% of billed tokens above 150k context | `docs/investigations/high-context-usage-2026-06-22.md` | yes | 1 | 1.70 ms |
| 3 | token usage levers | `docs/investigations/token-usage-levers-consolidated-2026-06-22.md` | yes | 2 | 1.41 ms |
| 4 | session transitions investigation | `docs/investigations/session-transitions-2026-07-10.md` | yes | 3 | 3.64 ms |
| 5 | build-phase halt allowlist | `.claude/rules/code-quality.md` | **NO** | - | 2.81 ms |

### Misses (honest record)

- **Q1** (`never dump secret file contents`) -> expected `docs/lessons-learned.md`.
  - Diagnosis: indexed but OUTRANKED (rank 17) -> ranking/corpus-composition.
  - Top-5 returned: `.claude/rules/security.md`, `docs/investigations/skill-deep-dives/plan-review/13-security-lens.md`, `docs/investigations/review-agents/11-auth-gated-runtime.md`, `docs/investigations/skill-deep-dives/session-wrap/24-lessons-learned-toc-maintenance.md`, `docs/investigations/skill-deep-dives/review-proof/06-unverifiable-claim-handling.md`.
- **Q5** (`build-phase halt allowlist`) -> expected `.claude/rules/code-quality.md`.
  - Diagnosis: indexed but OUTRANKED (rank 9) -> ranking/corpus-composition.
  - Top-5 returned: `docs/investigations/skill-deep-dives/build-phase/02-halt-contract.md`, `docs/investigations/ai-second-brain/c-action-agency/03-autonomy-vs-control.md`, `docs/investigations/skill-deep-dives/build-phase/01-autonomy-contract.md`, `docs/investigations/skill-deep-dives/build-phase/05-quality-gates.md`, `docs/investigations/ai-second-brain/c-action-agency/08-proactive-surfacing.md`.

## Exclusion

**Real-workspace secret absence -- PASS**

- No secret-shaped path (.env/secrets/*.pem/*.key/credentials/...) is in the index.
- No indexed body matches any secret content pattern (re-scanned every document).

**Fixture-tree exclusion -- PASS**

A throwaway tree with a `.env` secret (path-glob layer) and a Markdown file carrying a token-shaped secret (content-scan layer):

- keeper.md indexed: True (proves indexing works on the sample tree).
- .env fixture excluded (path-glob): True (code=secret-path-glob).
- token-bearing .md excluded (content-scan): True (code=secret-content).
- secret values absent from index bodies: True; absent from diagnostics: True.

Exclusion/adapter diagnostics observed on the real sample (by code):

- `exclusion/git-ignored`: 46
- `exclusion/oversized`: 1
- `exclusion/secret-path-glob`: 4
- `indexer/no-adapter`: 138
- `json/malformed-json`: 1
- `secret-scan/secret-content`: 2
- `secret-scan/undecodable-content`: 1

## Refresh correctness

**Delete + change reconciliation -- PASS**

- initial index: 3 files; deleted-marker present before=True, original-marker present before=True.
- after refresh: deleted doc removed=True, deleted-marker gone=True, old-content gone=True, new-content present=True.
- refresh counters: updated=1, skipped(unchanged)=1, deleted docs=1.

## Verdict

- Targets recovered in top-5: **3/5**; all within the 60s time-to-recovery budget: **True**.
- Exclusion (workspace + fixture): **PASS**.
- Refresh reconciliation: **PASS**.
- **Overall v1 verdict: FAIL.**

## v1.1 follow-ups

_Recorded per plan.md Step 6: this is a validation step; production `src/` was not changed. Genuine defects are captured here for a v1.1 pass._

- **Q1 -- ranking / corpus-composition (not a gap).** `docs/lessons-learned.md` is indexed and retrievable (rank 17 in the full corpus; #1 once the derivative brainstorm doc-sets are scoped out), but more-focused derivative docs outrank it under BM25 length normalization. Candidate improvements: a canonical-path authority boost for `rules/`/`plan` files, or an operator-facing scope filter. Query phrasing is fine.
- **Q5 -- ranking / corpus-composition (not a gap).** `.claude/rules/code-quality.md` is indexed and retrievable (rank 9 in the full corpus; #1 once the derivative brainstorm doc-sets are scoped out), but more-focused derivative docs outrank it under BM25 length normalization. Candidate improvements: a canonical-path authority boost for `rules/`/`plan` files, or an operator-facing scope filter. Query phrasing is fine.

